// GPC Online Library
// freedom_wars_(vitatv).gpc

// GPC Online Library
// ace_combat_infinity__use_the_aceedge_flight_stick_for_360.gpc

/* *
* GPC SCRIPT
* 
*  GPC is a scripting language with C-like syntax.
*  To learn more access GPC Online Help on Help menu.
* *********************************************************** */
int R1, R2, Square, L2;

main {
    //
    // The main procedure is called before every report be sent to
    // console, you can think in this procedure as a loop which only
    // ends when the script is unloaded.
    //
    R1 = get_val(PS3_SQUARE);
    R2 = get_val(PS3_R2);
    L2 = get_val(PS3_L2);

    if (R2 > R1) { R1 = R2; }
    set_val(PS3_SQUARE, R1);

    if (Square == 2){
        Square = 0;
    } else if (Square == 1) {
        if(L2>0) {set_val(PS3_CIRCLE, L2);}
    }
    Square = Square + 1;    
}
